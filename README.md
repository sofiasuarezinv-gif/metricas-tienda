# Métricas de Tienda

Dashboard de pedidos Dropi + rentabilidad. Subís el Excel de Dropi en la página; se guarda para siempre en GitHub y se de-duplica por ID.

## Render (Web Service)
- Build: `npm install` · Start: `node server.js`
- Env: `GH_TOKEN` (Contents R/W) y `GH_REPO=sofiasuarezinv-gif/metricas-tienda`
